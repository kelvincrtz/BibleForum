@model BibleForum.Models.Home.HomeIndexModel

@{
    ViewData["Title"] = "Home Page";
}

<div class="container body-content">
    <div class="row">
        <div class="col-md-10">

            <div class="">Learn Bible artifacts, share your biblical knowledge, bible stories and more!</div>
            <p class="">BibleForum is a New Zealand based bible forum website.</p>
            <div>
                <form asp-controller="Search" asp-action="Search" method="post">
                    <input asp-for="SearchQuery" type="text" class="" placeholder="Search..."/>
                </form>
            </div>
        </div>
        <div class="col-md-6"></div>
    </div>
    <div class="row">
        <h3>Latest Posts!</h3>
        <table class="table table-hover">
            <tbody>
                @foreach(var post in Model.LatestPosts)
                {
                    <tr>
                        <td>
                            <div class="forumLogo" style="background-image: url(@post.Forum.ImageUrl); background-size=100%"></div>
                            <div class="">
                                <a asp-controller="Post" asp-action="Index" asp-route-id="@post.Id">
                                    @post.Title
                                </a>
                            </div>
                            <div class="">
                                @if(post.RepliesCount == 1)
                                {
                                    <span>@post.RepliesCount</span>
                                }
                                @if(post.RepliesCount == 0)
                                {
                                    <span>No replies.</span>
                                }
                                else{
                                    <span>@post.RepliesCount replies</span>
                                }
                                <span class=""> 
                                    <a asp-controller="Profile" asp-action="Detail" asp-route-id="@post.AuthorId">
                                        @post.AuthorName
                                    </a>
                                </span>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>